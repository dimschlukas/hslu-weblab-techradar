version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongodb
    restart: always
    # ports:
    #   - 27017:27017
    volumes:
      # - mongo-data:/data/db
      - ./seed:/docker-entrypoint-initdb.d
    env_file:
      - .env
#volumes:
#  mongo-data:
